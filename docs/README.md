# 杭州麻将对战平台 Bot —— 项目文档

网易1024节赛事「AI 麻将大赛」参赛 Bot。目标：设计能夺冠的 AI。

## 目录结构

```
mahjong_bot/
├── mahjong/            # 算法内核 + 决策引擎（可单测）
│   ├── tiles.py        # 牌表示 + 协议字符串映射
│   ├── win.py          # 胡牌判定（回溯 + 财神百搭）
│   ├── shanten.py      # 向听数（含财神 + 七对 + 副露 melds 参数）
│   ├── ukeire.py       # 有效进张 / 听牌（含 melds）
│   ├── fan.py          # 番型计分（平胡/七对/豪华×N/爆头/七客/杠开/财飘/4白板，2026-09 新规则）
│   ├── decision.py     # 决策引擎（出牌/吃/碰/杠/飘财，含 melds）
│   └── game_state.py   # 局面状态（解析快照 + 牌墙剩余估算）
├── sim/                # 离线自对弈模拟器（量化胡牌率，A/B 策略）
│   ├── engine.py       # 牌墙/发牌/回合/吃碰杠暗杠补杠/财神/自摸胡/流局/计分
│   ├── players.py      # 策略接口 + Baseline + Smart
│   └── infer.py        # 牌墙剩余估算（uniform/empirical/suji 对手建模）
├── sim_run.py          # 模拟器 CLI（跑 N 局出胡牌率/平均分/番型）
├── sim_ab.py           # A/B 对比（相同牌墙隔离吃/碰/杠开关）
├── smart_bot.py        # 正式参赛 Bot（协议循环 + 决策接入）
├── create_room.py      # 创建测试房间并提取令牌
├── analyze_room.py     # 分析房间对局结果
├── replay_validate.py  # 离线回放验证
├── tests/              # 单元测试
└── data/               # 令牌、真实对局 events、日志
```

## 牌编码

| 花色 | 协议字符串 | 内核整数 |
|------|-----------|---------|
| 万 | `1w`-`9w` | 0-8 |
| 筒 | `1t`-`9t` | 9-17 |
| 条 | `1b`-`9b` | 18-26 |
| 字牌 | `东南西北中发白` | 27-33 |

财神 = 白板（33），百搭可替代任意牌。

> **2026-09-04 规则更新（已到 v11）**：v3 起七对允许财飘（最大番型 ×512）、爆头「摸任意牌即胡」、总番公式重写；v2 快照移除 allowed_actions（客户端自研动作判定）；v7 普通锦标赛多阶段化（stage_open/stage_done、跨阶段 ready、加赛）——smart_bot 主循环已多阶段化；v9 测试房数据 API 限速改 per-房间；v10 /state 跨局边界立即回全量快照；v11 state 轮询限速放宽至 16/s（smart_bot 提速至 ~12.5/s）。详见 docs/roadmap.md §5。权威番型端点：`POST /portal/api/tools/fan-calc`、规则版本：`GET /portal/api/guide/version`（均免认证）。

## 决策引擎策略（优先级从高到低）

1. **能胡就胡**
2. **杠**：有 gang 且 `wall_remaining > 20`（最后 10 墩禁杠）；暗杠/补杠/明杠均加速
3. **碰**：碰后（副露 + 打 1 张）向听数下降才碰
4. **吃**：吃后（副露 + 打 1 张）向听数下降才吃（最多 2 摊）
5. **飘财**：手牌「4 面子 + 2 财神」时打财神（×4 起）
6. **爆头路线**：财神做将向听数 ≤ 标准 + 1 时，保留财神走 ×2
7. **出牌**：向听数最小 → **二次进张深度**（一步前瞻）→ 番型价值
8. **不打财神**（白板百搭价值高）

注：向听数/进张计算已贯穿 `melds`（副露数），碰/吃/杠后手牌效率评估不再失真。

## 关键发现（调优过程中的实证结论）

- **向听数完全主导决策**：番型价值/进张质量等 tiebreaker 在 50 决策点 0 个改变决策
- **二次进张深度**：改变 14% 决策（是真正有效的 tiebreaker）
- **碰/杠/爆头/财飘都极罕见**（机会 0-1 次/30 局），是规则硬约束
- **财神做将（爆头）比当百搭慢 1-3 向听**，财神主要价值是「当百搭降向听数」
- **贝叶斯牌墙估算**：均匀缩放不改变决策排序，需对手建模才有效

## 关键数据洞察（80 个起手手牌统计）

- 财神分布：0 个 65%、1 个 25%、2 个 7.5%、3 个 2.5%
- 对子分布：4+ 对（七对潜力）仅 2.5%
- 起手向听数：集中在 3-4（67.5%）

**结论**：爆头/财飘（需「4 面子+财神」）与七对都很罕见，夺冠重点在「更快胡牌」（碰/杠加速 + 更强牌效率），而非罕见番型。

## 验证结果演进

| 版本 | 胡牌率（10局） |
|------|--------------|
| 最小 Bot | 0% |
| 基础牌效率 | 10% |
| +飘财 | 20% |

离线回放（50 决策点）：smart 决策 28% 更优、0% 更差。

### 离线自对弈 A/B（1500 局/配置，smart vs 3 baseline，相同牌墙）

| 配置 | 胡牌率 | 平均分 | 流局率 |
|------|-------|-------|-------|
| smart（全部吃碰杠） | **54.7%** | +12.98 | 41% |
| 去掉吃（nochi） | 49.7% | +11.49 | 46% |
| 去掉碰（nopeng） | 51.5% | +12.03 | 45% |
| 去掉杠（nogang） | 54.5% | +12.66 | 41% |

**结论**：吃（chi）是最大单一贡献（+5.0pp 胡牌率，显著）；碰（peng）次之（+3.2pp）；杠（gang）几乎无影响（+0.2pp，噪声级，但无害且杠开×2 有分数上限）。吃/碰通过加速成牌，把流局率从 46% 压到 41%。

### 番型/位置 A/B（1200 局/配置，相同牌墙）

| 配置 | 胡牌率 | 平均分 | 流局率 |
|------|-------|-------|-------|
| smart-ev（番型抵消 1 向听） | **55.1%** | +13.19 | 40% |
| smart-old（无番型抵消） | 54.7% | +12.97 | 41% |
| smart(全部, 番型抵消+庄家×8) | 54.4% | +12.94 | 41% |

**结论**：番型抵消 1 向听（EV 框架）小有收益（+0.4pp，弱但正向，保留）；庄家×8 位置策略**轻微有害**（-0.7pp）——庄家×8 同时放大赢和输，把番型权重也放大反而让庄家走更慢的番型路线、输得更多，方向搞反，**已关闭**（`dealer_aware=False`）。

**总体判断**：这三项（进张质量/EV框架/位置策略）都是弱信号，印证了「向听数完全主导决策」。真正动胡牌率的是吃/碰加速，而非番型或位置权衡。

## 待办（夺冠关键，详细见 docs/roadmap.md）

- [x] 吃决策（chi，最多 2 摊）—— `decision.should_chi`，已接入 smart_bot 与模拟器
- [x] 离线自对弈模拟器（`sim/`）—— 量化胡牌率，A/B 对比吃/碰/杠开关
- [x] 线上 bot 副露贯穿 —— smart_bot 本地跟踪副露数，`should_peng/chi` 与出牌按真实 melds 算向听
- [x] 进张质量（听牌宽度）+ EV框架（番型抵消 1 向听）—— 已实现并保留
- [x] 位置策略（庄家×8）—— 实测轻微有害，已关闭
- [ ] **P0 对手建模**：先做「上帝视角」诊断（传真实牌墙剩余），看 `remain` 非均匀化的上限收益
- [ ] **P1 吃/碰触发精细化**：向听数不变但进张质量提升时也吃碰；杠区分暗杠/明杠/补杠
- [ ] **P2 番型倍率精确化**：`calc_fan` 真实倍率替代 `_fan_value` 启发式
- [ ] **P3 锦标赛全局**：庄家×8 风险用法、M 场并发、连庄利用
